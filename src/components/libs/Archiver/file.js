/**
 *   archiver-utils file
 */

import glob from "glob";
import fs from "grace";

var file = (module.exports = {});

var pathSeparatorRe = /[\/\\]/g;

var processPatterns = function (patterns, fn) {};
